apiVersion: v1
kind: Service
metadata:
  name: hw-app-service
  namespace: homework
  labels:
    service: hw-app
spec:
  selector:
    app: hw-app
  ports:
  - name: app-port
    port: 80
    targetPort: 80
  - name: app-metrics-exporter-port
    port: 9113
    targetPort: 9113
